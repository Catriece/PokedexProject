// Potential file to be removed

// export function filterPokemonByType(list, clickedType) {
//     if (clickedType) return list.filter((info) => info.type === clickedType);
//     else return list;
// }
